<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exam Scheduler</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
        }

        h1 {
            color: #1e3c72;
            text-align: center;
            margin-bottom: 20px;
            font-size: 2.5em;
        }

        .nav-buttons {
            text-align: center;
            margin-bottom: 30px;
        }

        .nav-btn {
            padding: 10px 25px;
            margin: 0 10px;
            background: #6c757d;
            color: white;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            display: inline-block;
        }

        .nav-btn:hover {
            background: #5a6268;
        }

        .instructions {
            background: #e3f2fd;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 30px;
            border-left: 4px solid #1e3c72;
        }

        .instructions h3 {
            color: #1e3c72;
            margin-bottom: 10px;
        }

        .instructions ul {
            margin-left: 20px;
            color: #555;
        }

        .instructions li {
            margin: 5px 0;
        }

        /* Date Range Section */
        .date-section {
            background: #f8f9fa;
            padding: 25px;
            border-radius: 15px;
            margin-bottom: 30px;
            border: 2px solid #1e3c72;
        }

        .date-section h3 {
            color: #1e3c72;
            margin-bottom: 20px;
            font-size: 1.3em;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .date-inputs {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
        }

        .form-group label {
            margin-bottom: 8px;
            color: #495057;
            font-weight: 600;
            font-size: 0.95em;
        }

        .form-group input[type="date"],
        .form-group input[type="text"] {
            padding: 12px 15px;
            border: 2px solid #dee2e6;
            border-radius: 10px;
            font-size: 1em;
            transition: all 0.3s;
        }

        .form-group input:focus {
            outline: none;
            border-color: #1e3c72;
            box-shadow: 0 0 0 3px rgba(30, 60, 114, 0.1);
        }

        .date-info {
            background: #d1ecf1;
            padding: 15px 20px;
            border-radius: 10px;
            border-left: 4px solid #0c5460;
            margin-top: 15px;
            display: none;
        }

        .date-info p {
            color: #0c5460;
            font-size: 0.95em;
            margin-bottom: 5px;
        }

        .date-info strong {
            color: #062a30;
        }

        .time-slots {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-top: 20px;
        }

        .upload-section {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-bottom: 40px;
        }

        .upload-card {
            background: #f8f9fa;
            padding: 30px;
            border-radius: 15px;
            border: 2px dashed #1e3c72;
            transition: all 0.3s;
        }

        .upload-card:hover {
            border-color: #2a5298;
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }

        .upload-card h3 {
            color: #1e3c72;
            margin-bottom: 15px;
            font-size: 1.3em;
        }

        .file-input-wrapper {
            position: relative;
            overflow: hidden;
            display: inline-block;
            width: 100%;
        }

        .file-input-wrapper input[type=file] {
            position: absolute;
            left: -9999px;
        }

        .file-input-label {
            display: block;
            padding: 12px 20px;
            background: #1e3c72;
            color: white;
            border-radius: 8px;
            text-align: center;
            cursor: pointer;
            transition: background 0.3s;
        }

        .file-input-label:hover {
            background: #2a5298;
        }

        .upload-btn {
            width: 100%;
            padding: 12px;
            background: #28a745;
            color: white;
            border: none;
            border-radius: 8px;
            margin-top: 10px;
            cursor: pointer;
            font-size: 16px;
            transition: background 0.3s;
        }

        .upload-btn:hover {
            background: #218838;
        }

        .generate-section {
            text-align: center;
            margin: 40px 0;
        }

        .generate-btn {
            padding: 20px 60px;
            background: linear-gradient(135deg, #1e3c72 0%, #2a5298 100%);
            color: white;
            border: none;
            border-radius: 50px;
            font-size: 1.2em;
            cursor: pointer;
            box-shadow: 0 10px 30px rgba(30, 60, 114, 0.4);
            transition: all 0.3s;
        }

        .generate-btn:hover:not(:disabled) {
            transform: translateY(-3px);
            box-shadow: 0 15px 40px rgba(30, 60, 114, 0.6);
        }

        .generate-btn:disabled {
            background: linear-gradient(135deg, #6c757d 0%, #5a6268 100%);
            cursor: not-allowed;
            transform: none;
            opacity: 0.6;
        }

        .view-btn {
            padding: 15px 40px;
            background: #17a2b8;
            color: white;
            border: none;
            border-radius: 50px;
            font-size: 1.1em;
            cursor: pointer;
            margin-left: 20px;
            transition: all 0.3s;
        }

        .view-btn:hover {
            background: #138496;
            transform: translateY(-2px);
        }

        .message {
            padding: 15px;
            border-radius: 8px;
            margin: 20px 0;
            text-align: center;
            display: none;
        }

        .success {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .error {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }

        .warning {
            background: #fff3cd;
            color: #856404;
            border: 1px solid #ffeeba;
        }

        .file-name {
            margin-top: 10px;
            font-size: 0.9em;
            color: #666;
            font-style: italic;
        }

        @media (max-width: 768px) {
            .date-inputs,
            .time-slots {
                grid-template-columns: 1fr;
            }
            
            .container {
                padding: 20px;
            }
            
            h1 {
                font-size: 2em;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>📋 Exam Scheduler</h1>
        
        <div class="nav-buttons">
            <a href="/" class="nav-btn">📚 Class Timetable</a>
            <a href="/exam" class="nav-btn" style="background: #1e3c72;">📋 Exam Scheduler</a>
        </div>

        <div class="instructions">
            <h3>📋 CSV Format Instructions:</h3>
            <ul>
                <li><strong>Exam Courses CSV:</strong> code, name, credits, branch, year, students, type, faculty</li>
                <li><strong>Invigilators CSV:</strong> name</li>
                <li><strong>Exam Rooms CSV:</strong> number, capacity, type</li>
            </ul>
            <p style="margin-top: 10px; color: #d32f2f;"><strong>Important Rules:</strong></p>
            <ul style="margin-top: 5px;">
                <li>Branch: CSE, DSAI, ECE (branch-wise scheduling)</li>
                <li>Type: Theory, Lab (Labs will be excluded from exam schedule)</li>
                <li><strong>1 exam per day per branch</strong></li>
                <li><strong>Sundays automatically excluded from schedule</strong></li>
                <li>1 invigilator per room</li>
                <li>40 students per room</li>
                <li>Same day/room can be used by different branches</li>
            </ul>
        </div>

        <div id="message" class="message"></div>

        <!-- Date Range Section -->
        <div class="date-section">
            <h3>📅 Exam Date Range</h3>
            
            <div class="date-inputs">
                <div class="form-group">
                    <label for="startDate">Start Date *</label>
                    <input type="date" id="startDate" name="startDate" required>
                </div>
                
                <div class="form-group">
                    <label for="endDate">End Date *</label>
                    <input type="date" id="endDate" name="endDate" required>
                </div>
            </div>

            <div class="time-slots">
                <div class="form-group">
                    <label for="timeSlot1">Morning Session *</label>
                    <input type="text" id="timeSlot1" value="09:00 - 12:00" placeholder="e.g., 09:00 - 12:00" required>
                </div>
                
                <div class="form-group">
                    <label for="timeSlot2">Afternoon Session (Optional)</label>
                    <input type="text" id="timeSlot2" value="14:00 - 17:00" placeholder="e.g., 14:00 - 17:00">
                </div>
            </div>

            <div class="date-info" id="dateInfo">
                <p><strong>📊 Available Exam Days:</strong> <span id="examDays">0</span></p>
                <p><strong>🚫 Sundays Excluded:</strong> <span id="sundaysExcluded">0</span></p>
                <p><strong>📝 Note:</strong> One exam per day per branch. Sundays are automatically excluded from scheduling.</p>
            </div>
        </div>

        <div class="upload-section">
            <div class="upload-card">
                <h3>📚 Upload Exam Courses</h3>
                <form id="examCourseForm">
                    <div class="file-input-wrapper">
                        <label class="file-input-label" for="examCourseFile">
                            Choose CSV File
                        </label>
                        <input type="file" id="examCourseFile" name="file" accept=".csv" required>
                    </div>
                    <div class="file-name" id="examCourseName"></div>
                    <button type="submit" class="upload-btn">Upload Courses</button>
                </form>
            </div>

            <div class="upload-card">
                <h3>👨‍🏫 Upload Invigilators</h3>
                <form id="invigilatorForm">
                    <div class="file-input-wrapper">
                        <label class="file-input-label" for="invigilatorFile">
                            Choose CSV File
                        </label>
                        <input type="file" id="invigilatorFile" name="file" accept=".csv" required>
                    </div>
                    <div class="file-name" id="invigilatorName"></div>
                    <button type="submit" class="upload-btn">Upload Invigilators</button>
                </form>
            </div>

            <div class="upload-card">
                <h3>🏫 Upload Exam Rooms</h3>
                <form id="examRoomForm">
                    <div class="file-input-wrapper">
                        <label class="file-input-label" for="examRoomFile">
                            Choose CSV File
                        </label>
                        <input type="file" id="examRoomFile" name="file" accept=".csv" required>
                    </div>
                    <div class="file-name" id="examRoomName"></div>
                    <button type="submit" class="upload-btn">Upload Rooms</button>
                </form>
            </div>
        </div>

        <div class="generate-section">
            <button class="generate-btn" id="generateBtn" onclick="generateExamSchedule()">
                ⚡ Generate Exam Schedule
            </button>
            <button class="view-btn" onclick="window.location.href='/view-exam'">
                👁️ View Exam Schedule
            </button>
        </div>
    </div>

    <script>
        // Set minimum date to today
        const today = new Date().toISOString().split('T')[0];
        document.getElementById('startDate').min = today;
        document.getElementById('endDate').min = today;

        const startDateInput = document.getElementById('startDate');
        const endDateInput = document.getElementById('endDate');
        const dateInfo = document.getElementById('dateInfo');
        const examDaysSpan = document.getElementById('examDays');
        const sundaysExcludedSpan = document.getElementById('sundaysExcluded');
        const generateBtn = document.getElementById('generateBtn');

        // Calculate exam days excluding Sundays
        function calculateExamDays() {
            const start = new Date(startDateInput.value);
            const end = new Date(endDateInput.value);

            if (!startDateInput.value || !endDateInput.value) {
                dateInfo.style.display = 'none';
                return;
            }

            if (start > end) {
                showMessage('Start date must be before or equal to end date', 'warning');
                endDateInput.value = '';
                dateInfo.style.display = 'none';
                return;
            }

            let totalDays = 0;
            let sundays = 0;
            const current = new Date(start);

            while (current <= end) {
                if (current.getDay() === 0) { // Sunday = 0
                    sundays++;
                } else {
                    totalDays++;
                }
                current.setDate(current.getDate() + 1);
            }

            examDaysSpan.textContent = totalDays;
            sundaysExcludedSpan.textContent = sundays;
            dateInfo.style.display = 'block';

            if (totalDays < 5) {
                showMessage(`Only ${totalDays} exam days available. Consider extending the date range.`, 'warning');
            }
        }

        // Update end date minimum when start date changes
        startDateInput.addEventListener('change', () => {
            endDateInput.min = startDateInput.value;
            calculateExamDays();
        });

        endDateInput.addEventListener('change', calculateExamDays);

        function showMessage(msg, type) {
            const msgDiv = document.getElementById('message');
            msgDiv.textContent = msg;
            msgDiv.className = `message ${type}`;
            msgDiv.style.display = 'block';
            setTimeout(() => msgDiv.style.display = 'none', 5000);
        }

        // File name display handlers
        document.getElementById('examCourseFile').addEventListener('change', function(e) {
            document.getElementById('examCourseName').textContent = e.target.files[0]?.name || '';
        });

        document.getElementById('invigilatorFile').addEventListener('change', function(e) {
            document.getElementById('invigilatorName').textContent = e.target.files[0]?.name || '';
        });

        document.getElementById('examRoomFile').addEventListener('change', function(e) {
            document.getElementById('examRoomName').textContent = e.target.files[0]?.name || '';
        });

        // Upload handlers
        document.getElementById('examCourseForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const formData = new FormData(e.target);
            const response = await fetch('/upload/exam-courses', {
                method: 'POST',
                body: formData
            });
            const data = await response.json();
            showMessage(data.message || data.error, data.error ? 'error' : 'success');
            if (!data.error) {
                e.target.reset();
                document.getElementById('examCourseName').textContent = '';
            }
        });

        document.getElementById('invigilatorForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const formData = new FormData(e.target);
            const response = await fetch('/upload/invigilators', {
                method: 'POST',
                body: formData
            });
            const data = await response.json();
            showMessage(data.message || data.error, data.error ? 'error' : 'success');
            if (!data.error) {
                e.target.reset();
                document.getElementById('invigilatorName').textContent = '';
            }
        });

        document.getElementById('examRoomForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            const formData = new FormData(e.target);
            const response = await fetch('/upload/exam-rooms', {
                method: 'POST',
                body: formData
            });
            const data = await response.json();
            showMessage(data.message || data.error, data.error ? 'error' : 'success');
            if (!data.error) {
                e.target.reset();
                document.getElementById('examRoomName').textContent = '';
            }
        });

        // Generate exam schedule with date validation
        async function generateExamSchedule() {
            const startDate = startDateInput.value;
            const endDate = endDateInput.value;
            const timeSlot1 = document.getElementById('timeSlot1').value;
            const timeSlot2 = document.getElementById('timeSlot2').value;

            // Validate dates
            if (!startDate || !endDate) {
                showMessage('Please select both start and end dates', 'warning');
                return;
            }

            const examDays = parseInt(examDaysSpan.textContent);
            if (examDays < 1) {
                showMessage('No valid exam days available. Please select a different date range.', 'warning');
                return;
            }

            // Disable button and show loading
            generateBtn.disabled = true;
            generateBtn.textContent = 'Generating... Please wait';

            try {
                const response = await fetch('/generate-exam', { 
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({
                        startDate,
                        endDate,
                        timeSlot1,
                        timeSlot2
                    })
                });
                
                const data = await response.json();
                showMessage(data.message || data.error, data.error ? 'error' : 'success');
                
                if (!data.error) {
                    setTimeout(() => window.location.href = '/view-exam', 2000);
                }
            } catch (error) {
                showMessage('Failed to generate exam schedule: ' + error.message, 'error');
            } finally {
                generateBtn.disabled = false;
                generateBtn.textContent = '⚡ Generate Exam Schedule';
            }
        }
    </script>
</body>
</html>